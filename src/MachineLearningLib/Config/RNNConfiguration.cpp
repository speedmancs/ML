#include "RNNConfiguration.h"
namespace FengML
{
    bool RNNConfiguration::Load(const std::string& filePath)
    {
        return true;
    }

    bool RNNConfiguration::Save(const std::string& filePath)
    {
        return true;
    }
}